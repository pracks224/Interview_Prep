1. Go to home directory
   cd ~
2. Check .bash_profile
    nano ./.bash_profile
3. edit the file ; here I am setting the path for Maven
    export M2_HOME="/Users/mansisharma/Documents/apache-maven-3.9.1"
    PATH="${M2_HOME}/bin:${PATH}"
    export PATH
    3.1 To save the file use ctrl + O
4.  source the file to reflect the changes
    source ./.bash_profile
